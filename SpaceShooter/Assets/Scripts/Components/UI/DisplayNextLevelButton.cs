using UnityEngine.SceneManagement;
using UnityEngine;

public class DisplayNextLevelButton : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        if(SpaceShooterSceneManager.CurrentLevelCompleted == SceneManager.Cou)
    }
}
